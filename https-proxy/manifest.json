{
  "manifestVersion": 1,
  "meta": {
    "name": "HTTPS Proxy",
    "version": "0.1.3",
    "logoPath": "logo.png",
    "shortDescription": "HTTP/HTTPS reverse proxy for other pups (TLS optional; self-signed + web admin)",
    "longDescription": "Runs **plain HTTP by default** (matches Dogebox). Open the admin UI and enable **HTTPS** to terminate TLS with a **self-signed certificate** (default SAN **dogebox**; ports are not part of certs). Use **port listeners** so each listen port forwards to that pup’s upstream URL (e.g. `8080` → `http://127.0.0.1:8080`). If port listeners are empty, a single **PUBLIC_PORT** uses host-based routes. Admin is at `/__proxy/` on every listener. Download `cert.pem` from the admin API when HTTPS is on. This is **not** Let’s Encrypt.",
    "upstreamVersions": {
      "Go": "1.24"
    }
  },
  "config": {
    "sections": [
      {
        "name": "HTTP/TLS",
        "description": "Listen port on 0.0.0.0 (HTTP until you enable HTTPS in the admin UI). With no port listeners in the UI, this single port is used. If you add port listeners, each row has its own port (PUBLIC_PORT is ignored).",
        "fields": [
          {
            "name": "PUBLIC_PORT",
            "label": "Listen port (HTTP or HTTPS)",
            "type": "text",
            "default": "10000"
          }
        ]
      },
      {
        "name": "Storage & admin",
        "description": "Config and TLS material persist under HTTPS_PROXY_STORAGE. Set a fixed admin token to skip the generated admin.token file.",
        "fields": [
          {
            "name": "HTTPS_PROXY_STORAGE",
            "label": "Storage directory",
            "type": "text",
            "default": "/storage/https-proxy"
          },
          {
            "name": "HTTPS_PROXY_ADMIN_TOKEN",
            "label": "Admin API token (optional; empty = auto-generated file)",
            "type": "text",
            "default": ""
          }
        ]
      }
    ]
  },
  "container": {
    "build": {
      "nixFile": "pup.nix",
      "nixFileSha256": "ee13e5933568112b6cd43f4953d1342e83e0822e55accb6241349ab53ae3a2ad"
    },
    "services": [
      {
        "name": "https-proxy",
        "command": {
          "exec": "/bin/run.sh",
          "cwd": "",
          "env": null
        }
      }
    ],
    "exposes": [
      {
        "name": "https",
        "type": "http",
        "port": 10000,
        "interfaces": [
          "https-proxy-https"
        ],
        "listenOnHost": true,
        "webUI": true
      }
    ],
    "requiresInternet": false
  },
  "interfaces": [
    {
      "name": "https-proxy-https",
      "version": "0.1.3",
      "permissionGroups": [
        {
          "name": "HTTPS Proxy UI",
          "description": "Web admin and proxied HTTP/HTTPS traffic",
          "severity": 2,
          "routes": [
            "/*"
          ],
          "port": 10000
        }
      ]
    }
  ],
  "dependencies": [],
  "metrics": [
    {
      "name": "uptime_seconds",
      "label": "Uptime",
      "type": "int",
      "history": 30
    }
  ]
}
